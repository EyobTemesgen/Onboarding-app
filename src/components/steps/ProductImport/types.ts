export interface SampleProduct {
  sku: string;
  name: string;
  qty: number;
}

export interface ImportMethod {
  key: string;
  icon: string;
  title: string;
  description: string;
  infoText: string;
  infoIcon: string;
} 